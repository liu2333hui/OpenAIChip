

/*
class SystolicUnit(hardware_config){

PEArray_hc = hardware_config("PE_Array").toMap
PEArray_hc = hardware_config("PE_Array").toMap
PEArray_hc = hardware_config("PE_Array").toMap
PEArray_hc = hardware_config("PE_Array").toMap
PEArray_hc = hardware_config("PE_Array").toMap
PEArray_hc = hardware_config("PE_Array").toMap

array = new Module(PEArray(hardware_config))
adder_tree = new Module(AdderTree(hardware_config))
accumulator = new Module(Accumulator(hardware_config))

GMapper = new Module(GMapper(hardware_config))
AMapper = new Module(AMapper(hardware_config))
BMapper = new Module(BMapper(hardware_config))


Out_Loader = new Module(Accumulator(hardware_config))
Weight_Loader = new Module(Accumulator(hardware_config))
Input_Loader = new Module(Accumulator(hardware_config))
Weight_Cast = new Module(Accumulator(hardware_config))
Input_Cast  = new Module(Accumulator(hardware_config))

L2_Loader = new Module(Accumulator(hardware_config))

}



*/
